<Window x:Class="EzMarkdownViewer.UI.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About ez-markdown-viewer"
        Width="420" Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource Brush.Bg}"
        Foreground="{DynamicResource Brush.Fg}"
        TextOptions.TextFormattingMode="Display">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="ez-markdown-viewer"
                   FontSize="20"
                   FontWeight="SemiBold" />

        <TextBlock Grid.Row="1"
                   x:Name="VersionText"
                   Margin="0,4,0,0"
                   Opacity="0.8" />

        <TextBlock Grid.Row="2"
                   Margin="0,16,0,0"
                   TextWrapping="Wrap"
                   Text="A lightweight Windows desktop app for reading local markdown files." />

        <TextBlock Grid.Row="3" Margin="0,12,0,0">
            <Hyperlink x:Name="RepoLink"
                       NavigateUri="https://github.com/JohnReid87/ez-markdown-viewer"
                       RequestNavigate="RepoLink_RequestNavigate">
                https://github.com/JohnReid87/ez-markdown-viewer
            </Hyperlink>
        </TextBlock>

        <Button Grid.Row="4"
                Margin="0,16,0,0"
                Width="80"
                Height="28"
                HorizontalAlignment="Right"
                IsDefault="True"
                IsCancel="True"
                Content="OK"
                Click="Ok_Click" />
    </Grid>
</Window>
